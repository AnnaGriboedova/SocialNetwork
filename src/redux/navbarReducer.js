let initialState = {
    friends: [
        {id: 1, name: 'Anna'},
        {id: 2, name: 'Lusia'},
        {id: 3, name: 'Sasha'}
    ]
};

const navbarReducer = (state = initialState, action) => {
    switch (action.type) {

    }

    return state
};

export default navbarReducer;